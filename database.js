
//var mongoose = require('mongoose');
//var Schema = mongoose.Schema;

//var conferenceSchema = new Schema({
	//name:String,
	//conferenceCode: String,
	//flightDate: Date,
	//flightNumber: String,
//	arrivalTime: Number,
//	phone: Number
//});

//mongoose.model('conferenceSchema', conferenceSchema);
//mongoose.connect('mongodb://localhost:3000'); //made the database connection
